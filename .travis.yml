language: python
cache:
  - "pip"
python:
  - "3.6"
install:
  - "pip install coveralls nose2 prospector"
  - "pip install -r requirements.txt"
script:
  - "prospector"
  - "nose2 --with-coverage"
after_success:
  - "coveralls"
